import numpy as np


seed = 42

rng = np.random.default_rng(seed)

device_distances = rng.uniform(0, 20, size=10)

print(device_distances)
device_distances = rng.uniform(0, 20, size=10)
print(device_distances)
device_distances = rng.uniform(0, 20, size=10)
print(device_distances)